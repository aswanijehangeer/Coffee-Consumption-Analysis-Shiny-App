# Data Module
data_module <- function(input, output, session) {
  coffee_data <- reactive({
    # Importing the dataset (In current working directory)
    data <- read_xlsx("data/Coffee Caffeine Content.xlsx")
    # Cleaning Names
    data <- data |> clean_names()
    return(data)
  })
  
  return(coffee_data)
}
